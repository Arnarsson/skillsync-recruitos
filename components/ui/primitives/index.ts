/**
 * UI Primitives
 *
 * These components follow the design system defined in .design-engineer/system.md
 * They use borders-only depth, consistent spacing on 4px grid, and semantic colors.
 *
 * Usage:
 * import { Button, Card, Input, Badge, ProfileCard } from '@/components/ui/primitives';
 */

export { Button } from './Button';
export { Card, CardHeader, CardContent, CardFooter } from './Card';
export { Input, Textarea, Select } from './Input';
export { Badge, ScoreBadge } from './Badge';
export { ProfileCard, ProfileCardCompact } from './ProfileCard';
